# 运营后台 · 实施顺序与 Agent 投喂规范

- 文档版本：v0.1
- 日期：2026-09-18
- 性质：**开工指南**。第一部分回答「从哪一步开始、按什么顺序推进」；第二部分回答「每个任务给写码 Agent 喂什么资料」
- 事实来源：`OPS_ADMIN_REQUIREMENTS.md` v0.9、`DECISIONS.md` v1.7、`DATA_MODEL.md` v1.6、`ADMIN_REUSE_MAP.md` v0.2、`ADMIN_KNOWN_ISSUES.md` v0.1
- 配套：`prototype/admin/admin-mockup-v2.html`（18 屏形态）、`admin-mockup-v1.html`（规则论证）

---

# 第一部分 · 实施顺序

## 阶段总览

| 阶段 | 内容 | 是否写代码 | 阻塞关系 |
|---|---|---|---|
| **0** | 版本管理、决策裁定、环境就绪、账号初始化 | 否 | 阻塞全部后续 |
| **1** | 云函数基础设施（鉴权、审计、脱敏、二次验证） | 是 | 阻塞全部业务页 |
| **2** | 第一个纵切（A-15）+ 测试数据种子 | 是 | 解除「后台开发依赖 client 进度」 |
| **3** | 业务页面分六批 | 是 | 批内有序，批间大体独立 |
| **4** | 清理与上线前核对 | 少量 | 依赖阶段 3 完成 |

---

## 阶段 0 · 开工前（不写代码）

### 0.1 先把项目纳入 git ⚠️

`grouporder/` 整个目录**目前不在 git 跟踪下**。开始写代码前必须做，否则改坏无法回退。

```bash
cd /Users/sheng/Documents/HBuilderProjects/grouporder
git init                       # 若上层仓库已存在，改为在上层 git add grouporder/
printf 'node_modules/\nunpackage/\ndist/\n.DS_Store\n' > .gitignore
git add . && git commit -m "运营后台开工前基线：schema、菜单权限初始数据、效果图、架构文档"
```

**这是阶段 0 的第一件事，优先于其他所有动作。**

### 0.2 裁定两项决策

| 事项 | 影响 | 不裁定的后果 |
|---|---|---|
| **ADM-19** 业务页取数方式（方案甲云对象 / 方案乙受限 JQL） | 14 屏的实现方式与工作量 | 阶段 1 的基础设施没法设计，写了要返工 |
| **ADM-41** `uni-id-users.init_data.json` 的预置测试用户是否清空 | 初始化后是否留一条脏数据 | 初始化后再删要手工操作线上库 |

> 本文后续步骤按 **ADM-19 方案甲（业务页走云对象）** 编写。若改判方案乙，阶段 1 的分页/筛选约定与阶段 3 的全部云对象方法都需重做。

### 0.3 确认 P1-09 的开发策略

`DATA_MODEL.md` §12 把「微信服务类目与主体核验」标为**阻塞 schema 落地**。但核验迟迟不做会卡住全部开发。建议的折中：

- 在**测试服务空间**上传 schema 并开发，不影响任何线上资产
- 正式上线前完成核验；若类目要求调整产品形态，届时只改受影响的表

这是开发策略，需产品负责人认可后写入 DECISIONS。

### 0.4 上传服务端资产

按 `DATA_MODEL.md` §1 的约定：**只从 `grouporder-admin` 上传，`grouporder-client` 只拉取、永不上传。**

| 顺序 | 上传对象 | 注意 |
|---|---|---|
| 1 | DB Schema：19 张 `grouporder-*` + `uni-id-users` 覆盖版 | **不勾选 `uni-stat-*` 的 25 张表**（ADM-16） |
| 2 | 公共模块 `uni-config-center` | 含 2026-09-18 新生成的密钥，本地改动不上传不生效 |
| 3 | 公共模块 `uni-id-common` | |
| 4 | 云函数 `uni-id-co` | |
| 5 | 三份初始数据：`opendb-admin-menus`、`uni-id-roles`、`uni-id-permissions` | 执行前先确认初始化是全量覆盖还是增量合并（ADM-28） |

**不上传**：`uni-stat-cron`、`uni-stat-receiver`、`uni-analyse-searchhot`、`uni-upgrade-center`、`uni-sms-co`、`ext-storage-co`（ADM-16，`uni-stat-cron` 是定时任务，上传即计费）。

### 0.5 账号初始化（六步，见 `ADMIN_REUSE_MAP.md` §8）

1. 登录页 →「注册管理员账号」→ 创建首个 `admin` 账号（密码强度 `medium`：字母/数字/符号任两种，8–16 位）
2. 用 admin 登录
3. 在「运营账号」创建 4 个业务账号，分别授予 `ops-super` / `ops-content` / `ops-privacy` / `ops-auditor`
4. 给需要统计的账号额外勾选 `ops-stat-view`（默认不给任何角色）
5. **停用 admin 账号**（ADM-03，不做这步 OPS §3.1 的隔离不成立）
6. 之后一切操作使用 `ops-*` 账号

> 顺序要求：**必须先完成 0.4 的密钥上传，再注册账号**。`passwordSecret` 参与密码加密，先建账号后改密钥会导致密码失效。

### 0.6 阶段 0 验收

- [ ] 四个 `ops-*` 账号分别登录，左侧菜单与 `ADMIN_REUSE_MAP.md` §6.3 的可见菜单表逐项一致
- [ ] admin 账号已停用，用它登录被拒绝
- [ ] 未获 `ops-stat-view` 的账号看不到「运营统计」分组——**同时观察是否出现空壳分组**（验证 ADM-11 / ADM-23）
- [ ] 登录页只有用户名、密码、登录按钮（D-069）

---

## 阶段 1 · 云函数基础设施

**为什么先做这个**：OPS 有三条贯穿全部页面的硬约束——默认脱敏（§8.1）、每次提交重新鉴权（§3.1）、全部操作入审计（§13）。若不在基础设施层强制，14 个页面里必然有遗漏，而且事后补比一开始做贵得多。

### 1.1 产出物

```
uniCloud-alipay/cloudfunctions/
├── common/
│   └── grouporder-common/           公共模块（被各云对象 require）
│       ├── index.js                 导出下列能力
│       ├── auth.js                  鉴权：账号状态 + 角色 + 权限点
│       ├── oplog.js                 审计日志写入（9 字段）
│       ├── mask.js                  脱敏工具
│       ├── ops-verify.js            二次验证校验
│       ├── paging.js                分页/筛选/排序入参出参约定
│       └── errors.js                错误码
└── grouporder-ops-co/               运营端云对象（阶段 3 逐步填充方法）
    └── index.obj.js
```

### 1.2 六个基础能力的规格来源

| 能力 | 必须实现 | 规格位置 |
|---|---|---|
| **鉴权** | 以**操作提交时**的最新账号状态与权限为准，不依赖进入页面时的结果；账号停用/权限撤销立即失效 | OPS §3.1、§12.2 |
| **审计日志** | 9 个必含字段：日志编号、操作时间、操作账号 + **当时生效的角色**、动作类型、结果、对象类型与标识、关联事项、原因摘要、操作前后状态（无变化时明确记录）、请求唯一标识 | OPS §13；`DATA_MODEL.md` §10.8 |
| **脱敏** | 姓名 `张**`、电话 `138****2468`、地址 `深圳市南山区***`；列表、详情、搜索结果、日志摘要一律适用 | OPS §8.1、§8.2 |
| **二次验证** | 密码校验；有效期 1 天；免重验需**同一事项 + 同一授权对象 + 同一会话**三者同时满足；连续失败 5 次锁 15 分钟；不回显不记录密码 | D-068；OPS §8.2；注意 **ADM-20**（会话 2 小时，须同时判会话延续） |
| **分页约定** | 统一入参 `{page, pageSize, filters, orderBy}`、出参 `{list, total}`；对接 `uni-table` 的 `@filter-change` / `@sort-change` | ADM-19 方案甲的直接后果 |
| **错误码** | 区分：无权限 / 对象不存在 / 状态已变化 / 重复提交 / 二次验证失效，前端据此分别提示 | OPS §12.2 |

### 1.3 幂等与并发

OPS §12.2 要求「重复提交返回第一次成功结果或当前最终状态，不重复下架、恢复、解绑、授权或限制发布」。基础设施需提供统一的幂等键机制，业务方法直接复用。

### 1.4 阶段 1 验收

- [ ] 一个 `ping` 方法：四个角色分别调用，无权角色被拒绝且拒绝也写入审计
- [ ] 审计日志 9 字段齐全，`operator_roles` 记录的是调用当时的角色
- [ ] 脱敏工具对空值、单字姓名、非手机号格式不报错
- [ ] 二次验证：换事项必须重验；失败 5 次锁定；会话失效后旧授权不可用

---

## 阶段 2 · 第一个纵切与测试数据

### 2.1 A-15 运营账号页（推荐的第一个页面）

**选它的理由**：纯前端裁剪，不需要业务云对象，一天内能完成，且直接验证阶段 0 建立的权限模型是否正确。

五处必改见 `ADMIN_KNOWN_ISSUES.md` §2.1（ADM-02、04、05、06、07）。

### 2.2 测试数据种子

**问题**：后台治理的对象（活动、商品、订单、举报）都由 client 产生，而 client 尚未开工。若等 client，后台开发要停摆。

**做法**：写一个**仅测试环境可用**的种子云函数或本地 JQL 脚本，造出覆盖各状态的数据——

| 对象 | 需覆盖的状态 |
|---|---|
| 活动 | 草稿 / 审核中 / 进行中 / 已截止 / 已取消 × 治理正常 / 已下架；含「进行中 + 已下架」组合 |
| 商品 | 在售 / 停售 × 治理正常 / 已下架；`ever_ordered` 0 与 1；`total_stock` 0 与正整数 |
| 订单 | 有效 / 已取消 / 已作废；送货上门与自提各若干 |
| 举报 | 5 种状态各一条；含指向同一活动的多条 |
| 内容检测 | 文本通过 / 命中；图片待检 / 通过 / 已拦截 |

要求**幂等**（可重复执行不产生重复数据），且**绝不可上传到生产服务空间**。

### 2.3 阶段 2 验收

- [ ] A-15 只列持有 `ops-*` 角色的账号，无导出 Excel、无批量删除、无手机号邮箱列
- [ ] 自己那行的角色不可修改
- [ ] 种子脚本重复执行两次，数据量不变

---

## 阶段 3 · 业务页面分六批

批内按序，批间大体独立。每批产出「云对象方法 + 页面 + 菜单挂接」。

| 批 | 页面顺序 | 核心难点 |
|---|---|---|
| **A · 治理主链路** | A-07 详情 → A-09 下架/恢复 → A-08 举报 → A-10 发布者处置 | 双状态独立；**下架须反写商品库 `governance_blocked`（D-064，两条匹配取并集、软删记录同样封禁）**；恢复的三种禁止情形 |
| **B · 发布审核** | A-17 审核 → A-18 平台配置 | 结论绑定**提交时固化的内容版本**；图片异步检测不阻塞放行（D-058）；配置改动只影响之后新提交的审核（D-057） |
| **C · 检索与统计** | A-04 检索 → A-05 统计总览 → A-06 下钻 | 全链路脱敏；`ops-stat-view` 独立校验；指标白名单；**实测聚合耗时**（ADM-21） |
| **D · 隐私链路** | A-02 二次验证组件 → A-11 受控解敏 → A-12 绑定申诉 → A-16 隐私事项 | 授权只覆盖当前事项与对象（同活动其他订单仍脱敏）；双方均有业务数据时不受理；期限只读 |
| **E · 审计** | A-13 Excel 事件 → A-14 操作日志 | **A-13 不得有任何下载能力**（ADM-08）；A-14 无增删改；与登录日志分页不合并（ADM-13） |
| **F · 工作台** | A-03 | **必须最后做**——三档待办的条目来自前五批的对象状态，前面没完成就拼装不出来 |

### 阶段 3 的通用验收

每批完成后对照 OPS §14 的验收标准（8 组）中与该批相关的条目，以及 `ADMIN_KNOWN_ISSUES.md` 中该批涉及的条目。

---

## 阶段 4 · 清理与上线前核对

| 步 | 动作 | 依据 |
|---|---|---|
| 4.1 | A-03 完成后，清理 `pages/uni-stat/**`、`system/{app,tag}`、`uni-upgrade-center` 的页面与路由 | ADM-14（顺序不可颠倒） |
| 4.2 | `ADMIN_KNOWN_ISSUES.md` 41 条逐条过，标记处理或注明不处理的原因 | — |
| 4.3 | 上线前三项：密钥非 demo（ADM-01）、admin 已停用（ADM-03）、P1-09 类目核验完成 | — |
| 4.4 | OPS §14 八组验收标准逐项验证 | OPS §14 |
| 4.5 | 同步 `ADMIN_KNOWN_ISSUES.md` §6 的 5 处文档不一致 | ADM-31～35 |

---

# 第二部分 · 给写码 Agent 的投喂规范

## 核心原则

`docs/` 现有 **5000+ 行**正式文档加三份新架构文档。**整包丢给 Agent 是最差的做法**——上下文被淹没，关键约束反而被忽略。正确做法是**分层投喂 + 一张任务卡**。

| 原则 | 说明 |
|---|---|
| **引用章节，不是引用文件** | 写「OPS §6.1、D-064」，不写「读一下 OPS_ADMIN_REQUIREMENTS.md」 |
| **一次一个任务** | 一个页面或一组云对象方法。不要一次派 6 屏 |
| **每次都带 L0 底座** | 三条铁律不重复声明，就会被违反 |
| **先方案后代码** | 项目既定流程：分析 → 方案 → 确认 → 改代码 → 自查 → 总结 |
| **陷阱要点名** | 把该任务相关的 ADM 编号列出来，别指望 Agent 自己翻 41 条 |

## 三层投喂结构

```
L0 底座    每次必给，约 400 字，固定不变
   ↓
L1 规格    按任务选，精确到章节与 D 编号
   ↓
L2 参照    既有代码文件路径 + 效果图屏号
```

---

## L0 · 固定底座（每次任务原样附上）

> **项目**：群接龙购物小程序的**平台运营后台**（uni-admin + uniCloud）。它服务平台治理，**不服务团长经营**。
>
> **事实优先级**：`DECISIONS.md` → `PRD.md` / `OPS_ADMIN_REQUIREMENTS.md` → UX / 架构方案 → 子项目 README → 框架默认行为。冲突时按此顺序取信。
>
> **三条铁律**：
> 1. **默认脱敏**——列表、详情、搜索结果、日志摘要中的姓名/电话/地址一律脱敏（`张**`、`138****2468`、`深圳市南山区***`）。只有 A-11 走完「关联事项 + 填写原因 + 二次验证」后才显示完整字段，且只限该事项授权对象。
> 2. **业务状态与治理状态独立并列**——活动同时持有业务状态（草稿/审核中/进行中/已截止/已取消）与治理状态（正常/已下架），**相互不改写**。一个活动可以是「进行中 + 已下架」。禁止合并为一个状态字段或一个标签。
> 3. **运营不得代替团长经营**——全站不得出现创建/编辑/取消活动、增删改商品、改价格库存限购截止时间、新建或修改订单、改统计数字的入口。运营只能：审核、下架、恢复、警告、限制。
>
> **数据访问**：19 张 `grouporder-*` 表的 schema `permission` 全为 `false`，**客户端不可直连**。所有业务数据读写走云对象，`uni-table` 手动喂数据。不要写 `<unicloud-db :collection="grouporder-*">`。
>
> **工作方式**：先输出实施方案等确认，再改代码；改完自查空值风险、生命周期、命名、项目规范。**发现规格矛盾或缺口先提出，不要自行裁决**——项目既定规则是「项目文件是唯一事实来源，聊天内容只有写入文档并经确认后才成为项目结论」。

---

## L1 + L2 · 任务卡模板

```markdown
## 任务：<编号与名称>

### 范围
做：<具体到页面/方法名>
不做：<明确排除项，防止范围蔓延>

### 规格依据（按此实现，冲突时以列在前者为准）
- OPS §x.x —— <一句话说明这节管什么>
- D-0xx —— <决策要点>
- DATA_MODEL §x.x —— <涉及的表与字段>
- UX_FLOW_SPEC <页面编号> / UX_STATE_MATRIX §x —— <状态与流转>

### 形态参照
- `prototype/admin/admin-mockup-v2.html` 第 <n> 屏（<页面编号>）
- 视觉与交互规范：`ADMIN_REUSE_MAP.md` §2（骨架、表格、按钮、筛选位置的实测值）

### 复用与代码参照
- 复用定性：<◆复用 / ◇改造 / ○新建>，见 `ADMIN_REUSE_MAP.md` §3
- 表格写法参照：`pages/system/role/list.vue`
- 基础设施：`common/grouporder-common`（鉴权、审计、脱敏、分页）

### 已知陷阱（必须处理）
- ADM-xx：<一句话>
- ADM-xx：<一句话>

### 验收标准
- [ ] <从 OPS §14 或 SPEC 的 AC 条目摘录，可验证>
- [ ] <每条都要能被实际操作验证>

### 输出要求
先给实施方案（改动哪些文件、关键逻辑、风险点），确认后再写代码。
```

---

## 三张示例任务卡

### 示例 A · 云函数基础设施（阶段 1）

```markdown
## 任务：grouporder-common 公共模块 + grouporder-ops-co 云对象骨架

### 范围
做：六个基础能力——鉴权、审计日志、脱敏、二次验证校验、分页约定、错误码；
    外加一个 ping 方法用于验证链路。
不做：任何业务方法（举报、审核、下架等一律留到阶段 3）。

### 规格依据
- OPS §3.1 —— 鉴权以操作提交时的最新账号状态与权限为准，不依赖进入页面时的结果
- OPS §13 —— 审计日志 9 个必含字段，覆盖 11 类事件；不得记录密码与凭证
- OPS §8.1 §8.2 —— 脱敏范围与受控访问规则
- D-068 —— 二次验证：密码校验、1 天有效、同一事项+同一对象+同一会话、失败 5 次锁 15 分钟
- OPS §12.2 —— 错误态分类；重复提交返回首次结果或当前最终状态
- DATA_MODEL §10.8 / §10.10 —— grouporder-oplog 与 grouporder-ops-verify 表结构

### 形态参照
无界面。

### 复用与代码参照
- 云对象写法参照 uniCloud 官方 importObject 规范
- 鉴权可复用 uni-id-common 的 checkToken，但**角色与权限点校验需自行实现**

### 已知陷阱
- ADM-20：二次验证有效期 1 天，但 web 端 token 仅 2 小时（`tokenExpiresIn: 7200`）。
  `expire_time` 按 +1 天写，但校验时**必须同时判定会话是否延续**，否则重新登录后仍免验证，违反 OPS §8.2。
- ADM-10：自定义权限点只控菜单可见性，不控数据访问；云对象内必须自行校验权限点。

### 验收标准
- [ ] 四个 ops-* 角色分别调用 ping，无权角色被拒绝，**且拒绝也写入审计日志**
- [ ] 审计日志 9 字段齐全，operator_roles 是调用当时的角色而非当前查询结果
- [ ] 二次验证换事项必须重验；连续失败 5 次锁定 15 分钟；日志中不含密码
- [ ] 脱敏对空值、单字姓名、非手机号格式输入不抛异常

### 输出要求
先给方案（模块划分、导出接口签名、审计字段映射），确认后写代码。
```

### 示例 B · 复用裁剪类（阶段 2）

```markdown
## 任务：A-15 运营账号页（复用 pages/system/user/list.vue 并裁剪）

### 范围
做：在现有页面上完成五处裁剪 + 角色列映射。
不做：不重写页面、不改 uni-id-users schema、不碰 system/role 与 system/permission 两页。

### 规格依据
- OPS §3.1 §3.2 §4.9 —— 账号只能由超管创建；4 角色分别授权；ops-stat-view 是独立权限点
- D-038 —— 账号体系复用 uni-id，小程序用户与运营账号**同表**
- D-029 —— 账号不采集手机号

### 形态参照
- v2 效果图第 15 屏（A-15），含页面顶部的「五处必改」对照表

### 复用与代码参照
- ◆ 复用 + ◇ 改造，见 ADMIN_REUSE_MAP.md §4
- 本页可继续用 unicloud-db 直连（uni-id-users 是 read: true，不受 19 张业务表的限制）

### 已知陷阱（五处必改，照搬即违规）
- ADM-02 🔴 `whereState = ref('')` 空条件会列出**全部小程序用户**（估算 5 万）。必须加运营角色过滤，只列持有 ops-* 的账号。
- ADM-04 删除 `<download-excel>` 导出按钮（OPS §15）
- ADM-05 隐藏手机号码、邮箱两列（D-029）
- ADM-06 角色列映射为 4 个中文角色名，ops-stat-view 单列
- ADM-07 删除批量删除，只保留停用（OPS §3.1 只提停用）

### 验收标准
- [ ] 列表只出现持有 ops-* 角色的账号，小程序用户一条不显示
- [ ] 无导出 Excel 按钮、无批量删除按钮、无手机号与邮箱列
- [ ] 当前登录账号自己那行的角色复选框为禁用态
- [ ] 停用某账号后，该账号已打开的页面下一次操作被拒绝

### 输出要求
先说明改动点与 where 条件的具体写法，确认后修改。
```

### 示例 C · 新建业务页类（阶段 3 批 B）

```markdown
## 任务：A-17 活动发布审核（页面 + 云对象方法）

### 范围
做：待审队列、审核详情、通过/不通过结论提交；对应云对象方法。
不做：不做 A-08 举报处置（两条独立流程，页面不合并）；不做 A-18 平台配置。

### 规格依据
- OPS §4.3 §5.2 —— 发布前拦截；队列来源由平台配置决定；结论绑定内容版本
- D-043 —— 审核通过前不对外开放、不可分享、不可下单；不通过退回草稿；审核中可撤回
- D-057 —— 审核模式（自动/人工）在**提交审核时固化**到 activity.review_mode，之后改配置不影响已在审核中的活动
- D-058 —— 图片异步检测**不阻塞审核放行**；放行后回调命中转治理下架，不回退业务状态、不删订单
- D-059 —— 活动不进入任何公开列表
- DATA_MODEL §4.1 —— activity 表的 status / review_* / content_version 字段
- UX_FLOW_SPEC A-17、F-A03；UX_STATE_MATRIX §8「发布审核」行

### 形态参照
- v2 效果图第 17 屏（A-17）

### 复用与代码参照
- ○ 新建。复用 uni-table（列头筛选）、uni-forms、uni-notice-bar
- 取数走 grouporder-ops-co，分页筛选按阶段 1 的统一约定
- 基础设施：鉴权校验 ops-content-review 权限点；结论提交写审计

### 已知陷阱
- ADM-12：菜单权限只控入口。本页数据范围需在云对象内按角色过滤。
- ADM-19：不得使用 unicloud-db 直连 grouporder-activity。

### 验收标准
- [ ] 审核通过后活动进入「进行中」并产生分享入口；不通过退回草稿并向团长展示原因
- [ ] 原因字段在「不通过」时必填，且内容不含内部敏感信息
- [ ] 团长在审核期间编辑产生新版本并重新排队，**运营不能用新版本内容替换正在审核版本的结论**
- [ ] 页面上活动标题、说明、价格、库存、限购、截止时间**全部只读，无任何编辑入口**
- [ ] 图片检测仍在「检测中」时也能正常放行（不阻塞）
- [ ] 提交时状态已被他人改变 → 展示最新状态与处理人，不用旧页面覆盖

### 输出要求
先给方案（云对象方法签名、状态流转校验点、审计字段），确认后写代码。
```

---

## 反面清单（这样投喂会出问题）

| ❌ 做法 | 后果 |
|---|---|
| 「先把 docs 目录全读一遍，然后写 A-08」 | 5000+ 行淹没重点，关键约束被忽略；Token 消耗大且注意力分散 |
| 「按效果图把 18 屏都实现了」 | 范围过大，基础设施缺失导致每页重复实现鉴权与脱敏，最终不一致 |
| 只给效果图不给规格 | 图只表达形态，脱敏规则、状态机、幂等要求都在文档里 |
| 只给规格不给效果图 | 会自造一套 UI，重演本项目 v1 的教训（自建框架而非复用 uni-admin） |
| 不点名 ADM 陷阱 | 41 条里与本任务相关的通常 2～4 条，不点名基本会踩 |
| 不给验收标准 | 无法判断完成度，返工率高 |
| 允许 Agent 自行裁决规格矛盾 | 产生第二个事实源，与项目既定规则冲突 |

---

## 变更记录

| 日期 | 版本 | 变化 |
|---|---|---|
| 2026-09-18 | v0.1 | 建立实施顺序（5 阶段）与 Agent 投喂规范（三层结构 + 任务卡模板 + 3 张示例 + 反面清单） |
